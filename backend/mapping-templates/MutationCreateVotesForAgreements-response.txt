#set( $agreements = [] )
#set( $results = $context.result.data.Vysae)

#foreach($res in $results)
	#set ($hyphen = $res.PK.indexOf('-'))
	#set ($substring = $res.PK.substring(0, $hyphen))
	#if($substring == "Agreement")
        #set( $agreement = {
        	"id": "$res.PK",
            "votes": [{
            	"id": "$res.SK",
                "result": "$res.result"
            }]
        })
        
      $util.qr($agreements.add($agreement))
    #end
#end

$util.toJson($agreements)
