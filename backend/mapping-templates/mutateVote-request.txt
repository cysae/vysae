#set($uuid = $util.autoId())
#set($batchData = [])
$util.qr($batchData.add($util.dynamodb.toMapValues({ "PK": "Vote-$uuid", "SK": "Vote-$uuid", "result": "${context.arguments.vote.result}" })))
$util.qr($batchData.add($util.dynamodb.toMapValues({ "PK": "${context.arguments.agreementId}", "SK": "Vote-$uuid", "result": "${context.arguments.vote.result}" })))
{
  "version" : "2018-05-29",
  "operation" : "BatchPutItem",
  "tables": {
    "Vysae": $utils.toJson($batchData)
  }
}
